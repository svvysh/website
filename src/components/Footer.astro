---
import { SITE_DOMAIN } from '@/lib/constants'

const currentYear = new Date().getFullYear()
---

<footer class='relative z-20 border-t border-border px-3 sm:px-6 py-2.5 sm:py-3 bg-muted/20'>
  <div
    class='flex flex-col sm:flex-row items-center justify-between gap-1.5 sm:gap-3 text-[9px] sm:text-xs text-muted-foreground font-mono'
  >
    <span>© {currentYear} {SITE_DOMAIN}</span>
  </div>
</footer>
